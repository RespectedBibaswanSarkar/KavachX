import React from 'react';
import ReactDOM from 'react-dom/client';
import LiveThreats from './pages/LiveThreats.jsx';
import './styles/globals.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <LiveThreats />
    </React.StrictMode>
);
