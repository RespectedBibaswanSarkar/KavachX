"""
requirements.txt - All dependencies with graceful fallback notes

Core dependencies (always required):
  - numpy: Numerical computing
  - scipy: Scientific computing

ML/AI (optional but recommended):
  - torch: Deep learning (RL agent)
  - scikit-learn: Classical ML (IsolationForest, preprocessing)
  - xgboost: Gradient boosting (fallback to RandomForest without it)
  - shap: Model explanations (fallback to feature importance without it)

Web API (optional but recommended):
  - fastapi: REST framework
  - uvicorn: ASGI server
  - pydantic: Data validation

The system gracefully degrades if optional libraries are not available.
"""

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0

# ML/AI (optional)
torch>=1.9.0
scikit-learn>=1.0.0
xgboost>=1.5.0
shap>=0.40.0

# Web API (optional)
fastapi>=0.68.0
uvicorn>=0.15.0
pydantic>=1.8.0

# Additional useful packages
python-multipart>=0.0.5
